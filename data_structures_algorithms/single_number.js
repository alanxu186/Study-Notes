//Given a non-empty array of integers nums, every element appears twice except for one. Find that single one. 
//Must have linear runtime complexity and use constant extra space

/**
 * @param {number[]} nums
 * @return {number}
 */

var singleNumber = function(nums){

};